topics:
  scan: /scan
  odom: /odom
  cmd_vel: /cmd_vel
  cmd_vel_type: TwistStamped

env:
  lidar_beams: 60
  lidar_max_range: 3.5
  control_hz: 10
  dt: 0.1
  max_steps: 600
  v_max: 0.22
  w_max: 2.0
  r_safe: 0.30

reward:
  time_penalty: -0.5
